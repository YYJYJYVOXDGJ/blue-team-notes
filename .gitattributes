# 默认按文本处理并统一为 LF：避免 Windows 检出/提交造成的全文件行尾噪音
* text=auto eol=lf

# Shell 脚本必须 LF，否则 shebang 失效（报 \r: command not found）
*.sh  text eol=lf
*.bash text eol=lf

# Windows 脚本用 CRLF
*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf
