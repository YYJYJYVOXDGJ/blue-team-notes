# 贡献指南

> 本仓库以**文档为主、脚本为辅**，规范的重点在「可读、可查、可校验」。提交前请过一遍本文的 [§4 提交前自检清单](#4-提交前自检清单)。

- [1. 内容原则](#1-内容原则)
- [2. 文档规范](#2-文档规范)
- [3. 脚本规范](#3-脚本规范)
- [4. 提交前自检清单](#4-提交前自检清单)
- [5. 提交信息规范](#5-提交信息规范)
- [6. 新增文档放哪里](#6-新增文档放哪里)
- [7. 授权与免责](#7-授权与免责)

---

## 1. 内容原则

1. **命令必须可执行、可判读。** 每条命令都要说明「查什么」和「结果怎么看」，只列命令不写判读的提交不予合并。
2. **标注权限与风险。** 需要 `sudo`/管理员权限的命令要标注；有破坏性的命令必须加明确警告。
3. **不做「全覆盖」的假承诺。** 覆盖不到的地方要显式写出（例如 `windows-detection-validation.md` §7 的已知盲区）。诚实是这类文档的核心价值。
4. **原生工具优先。** 能用系统自带命令解决的不引入第三方依赖；引入第三方工具时给出获取方式与用途。
5. **区分「已覆盖 / 部分 / 待建设」。** 新增能力时同步更新 `ir/ir-framework.md` §3 的能力地图。
6. **不收录攻击性内容。** 不提供漏洞利用代码、恶意样本、免杀或规避检测的具体实现。手法描述以「识别与处置」为目的。

## 2. 文档规范

### 2.1 结构

- 每个文档以一级标题（`#`）开头，紧跟一段 `>` 引用块说明**这份文档解决什么问题**；
- 超过 6 个章节的文档需要目录，锚点格式与正文标题一致（GitHub 会自动小写并去标点）；
- 章节编号用 `## 1.` / `### 1.1` 两层，不再往下细分；
- 跨文档引用统一写法：`windows-host-audit.md` §3.3（文件名 + 章节号，不要只写章节号）。

### 2.2 格式

| 项 | 规则 |
|---|---|
| 代码块 | 必须标注语言（`bash` / `powershell` / `bat` / `yaml` / `text` / `ini`）；围栏必须成对闭合 |
| 表格 | 用于枚举与对照；列数控制在 7 列以内，过宽改用「小标题 + 列表」 |
| 检查项 | 用 `- [ ]` 任务列表，便于现场逐项打勾 |
| 命令注释 | 用 `#` 注释说明该条命令在查什么、异常长什么样 |
| 禁止 | 不要使用 HTML 标签、内联样式、图片外链；不要写裸 URL 作为命令来源（放到「参考」段落） |
| 中文标点 | 正文使用中文标点；代码、路径、命令、字段名保持 ASCII 原样 |

### 2.3 行尾与编码（**历史踩坑，务必遵守**）

| 文件类型 | 行尾 | 编码 |
|---|---|---|
| `.md` `.yml` `.json` `.txt` | **LF** | UTF-8（无 BOM） |
| `.sh` `.bash` | **LF** | UTF-8（无 BOM）——CRLF 会导致 shebang 失效（`\r: command not found`） |
| `.ps1` `.bat` `.cmd` | **CRLF** | UTF-8 **with BOM**——PowerShell 5.1 读取无 BOM 的 UTF-8 脚本会按 ANSI 解析，中文注释乱码甚至语法报错 |

根目录 `.gitattributes` 已固定上述规则。若发现工作区行尾异常，执行：

```bash
git add --renormalize .
```

## 3. 脚本规范

### 3.1 通用

1. **只读优先。** 采集与核查类脚本严禁修改系统配置、杀进程、改防火墙；确需变更加固项时，单独成脚本并在头部写明风险。
2. **输出到独立目录**，不要散落在当前目录；结尾给出**结论摘要**（发现的问题、计数、需要人工确认的项）。
3. **不做静默失败。** 管道后接 `|| echo "OK"` 的写法是错的——管道返回的是最后一个命令的退出码，`||` 几乎不会触发。用函数封装输出并由函数判断：

```bash
report() {
  local out="$1"
  if [ -n "$out" ]; then printf '%s\n' "$out" | sed 's/^/  [!] /'; else echo "  OK"; fi
}
report "$(grep -vE '^(root|sync)$' /etc/passwd)"
```

4. **兼容性**：Linux 脚本以 `#!/usr/bin/env bash` 开头并启用 `set -uo pipefail`（谨慎使用 `-e`，采集脚本中途退出会丢证据）；PowerShell 脚本注明最低版本（5.1 / 7+）。

### 3.2 校验方式

```bash
# Shell 语法
bash -n scripts/xxx.sh
# 或使用 ShellCheck
shellcheck scripts/xxx.sh
```

```powershell
# PowerShell 语法（比依赖外部 exe 更可靠）
$t=$null; $e=$null
[System.Management.Automation.Language.Parser]::ParseFile($f, [ref]$t, [ref]$e)
$e   # 应为空
```

## 4. 提交前自检清单

- [ ] 代码围栏成对闭合（`grep -c '^```' 文件` 结果为**偶数**）
- [ ] 目录锚点与标题一致（改标题后必须同步目录）
- [ ] 章节编号连续，无跳号、无重号
- [ ] 行尾与编码符合 [§2.3](#23-行尾与编码历史踩坑务必遵守)
- [ ] Shell 脚本 `bash -n` 通过；PowerShell 脚本 `ParseFile` 无 error
- [ ] 若修改了已有手册：**原有命令未被删除**（可用关键字清单逐一比对）
- [ ] 新增能力已同步到 `ir/ir-framework.md` §3 能力地图
- [ ] 新增文档已加入 `README.md` 的仓库结构与文档地图
- [ ] 变更已记入 `CHANGELOG.md`（面向使用者的变更）
- [ ] 全文检索一遍是否有占位符、临时路径、个人敏感信息遗留

## 5. 提交信息规范

采用类似 Conventional Commits 的前缀 + 中文描述：

| 前缀 | 用途 |
|---|---|
| `docs:` | 文档新增或修改 |
| `feat:` | 新增脚本或能力 |
| `fix:` | 修正错误命令、错误判读、脚本缺陷 |
| `refactor:` | 结构调整（不改内容含义） |
| `chore:` | 仓库治理（`.gitignore`、`.gitattributes`、格式） |

要求：

- 首行不超过 72 字符，说明**做了什么**；
- 正文用 `-` 列要点，说清**为什么**做、影响哪些文件；
- 一次提交只做一件事：内容变更与仓库治理分开提交。

示例：

```text
docs: 新增 Windows 检测能力验证清单

- 把主手册检测逻辑转为 V-01~V-63 可执行用例，含期望日志与命中判定标准
- 补充 14 项日志源核查表，先排除「日志源未开导致假阴性」
- 标注 8 项已知盲区，明确不覆盖范围
```

## 6. 新增文档放哪里

| 内容类型 | 位置 | 命名 |
|---|---|---|
| 应急响应体系、流程、场景、模板 | `ir/` | `ir-<主题>.md`（如 `ir-playbook.md`） |
| 平台专项（排查手册、攻击映射、基线、工具链） | `linux/` 或 `windows/` | `<平台>-<主题>.md` |
| 自动化脚本 | `scripts/` | Linux：`<用途>.sh`；Windows：`<用途>.ps1` |

新增平台（如 `kubernetes/`、`cloud/`）时，建议先提出 Issue 讨论结构，避免目录风格分裂。

## 7. 授权与免责

- 提交即表示你同意以本仓库 `LICENSE` 的条款发布你的贡献；
- 所有贡献内容须符合 `DISCLAIMER.md` 的用途限定与禁止事项；
- 贡献者在文档中提供的检测用例，须为**可在隔离环境安全复现**的用例，并在文档中标注风险等级。
